document.addEventListener('DOMContentLoaded', function() {
    // Controle básico do vídeo - removido o overlay complexo
    const youtubePlayer = document.getElementById('youtubePlayer');
    
    if(youtubePlayer) {
        // Pode adicionar controles simples aqui se necessário
    }
    
    // Removido o efeito de digitação conforme solicitado
});